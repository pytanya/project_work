# ==============================================================================
# EduTutor — зависимости (контракт по разделу 11.1 SPECIFICATION.md)
# Устанавливаются постепенно, по слайсам разработки:
#   Слайс 0 (фундамент): pydantic, pydantic-settings, rich, pytest, pytest-asyncio, httpx
#   Слайс 1 (LLM+guardrails): openai, python-dotenv
#   Слайс 3 (intake): langgraph, langgraph-checkpoint-sqlite
#   Слайс 4 (RAG): docling, pdfplumber, chromadb, sentence-transformers, cross-encoder
#   Слайс 5 (source_finder): crawl4ai, playwright, ddgs, beautifulsoup4, lxml
#   Слайс 5+ (API-схемы уже в слайсе 0; сервер FastAPI — расширение): fastapi, uvicorn
#   Слайс 6 (observability): arize-phoenix, openinference-instrumentation-openai
#
# ==============================================================================
# СИСТЕМНЫЕ ТРЕБОВАНИЯ (кросплатформенно)
# ----------------------------------------
# torch (sentence-transformers) и chromadb (rust-binding) требуют системных
# компиляторов/рантаймов. Без них работают альтернативные бэкенды по умолчанию:
#   EMBEDDING_PROVIDER=api (RouterAI /embeddings) и VECTOR_STORE=numpy.
#
#   Windows — установить Microsoft Visual C++ Redistributable (x64):
#       https://aka.ms/vs/17/release/vc_redist.x64.exe
#     (нужен для локальных embeddings sentence-transformers и ChromaDB;
#      также требуется для сборки любой C-extension из исходников)
#
#   macOS — установить Command Line Tools (Xcode):
#       xcode-select --install
#
#   Linux — установить build-essential (gcc/make) и python3-dev:
#       Debian/Ubuntu:  sudo apt-get update && sudo apt-get install -y build-essential python3-dev
#       Fedora/RHEL:    sudo dnf groupinstall "Development Tools" && sudo dnf install python3-devel
# ==============================================================================

# --- Фундамент (Слайс 0) ---
pydantic>=2.7.0
pydantic-settings>=2.2.0
rich
pytest
pytest-asyncio
httpx
python-dotenv

# --- LLM-клиент (Слайс 1) ---
openai>=1.0
requests

# --- Intake / граф (Слайс 3) ---
langgraph>=0.2
langgraph-checkpoint-sqlite

# --- RAG / обработка документов (Слайс 4) ---
# ВНИМАНИЕ: torch (sentence-transformers) и chromadb (rust) требуют системного
# рантайма/компилятора: Windows — VC++ Redistributable (aka.ms/vs/17/release/vc_redist.x64.exe),
# macOS — xcode-select --install, Linux — build-essential (см. шапку файла).
# Без них работают альтернативные бэкенды (по умолчанию):
#   EMBEDDING_PROVIDER=api (RouterAI /embeddings) и VECTOR_STORE=numpy.
docling
pdfplumber
chromadb
sentence-transformers
# Qdrant векторное хранилище (roadmap #1; VECTOR_STORE=qdrant).
# qdrant-client поддерживает embedded-режим (QDRANT_PATH) без сервера/Docker.
qdrant-client
# OCR сканированных учебников (3.2) — страницы по запросу ученика
easyocr

# --- Сбор источников (Слайс 5) ---
crawl4ai
playwright
ddgs
beautifulsoup4

# --- Observability (Слайс 6) ---
arize-phoenix
openinference-instrumentation-openai

# --- API (расширение заказчика) ---
fastapi
uvicorn

# --- Граф знаний (NetworkX) ---
networkx
